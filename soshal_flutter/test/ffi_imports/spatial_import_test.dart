// Import test for ffi wrapper: spatial
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import spatial compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
