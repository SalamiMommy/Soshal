// Import test for ffi wrapper: media
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import media compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
