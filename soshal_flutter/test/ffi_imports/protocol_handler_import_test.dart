// Import test for ffi wrapper: protocol_handler
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import protocol_handler compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
