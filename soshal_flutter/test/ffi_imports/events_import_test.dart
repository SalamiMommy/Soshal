// Import test for ffi wrapper: events
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import events compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
