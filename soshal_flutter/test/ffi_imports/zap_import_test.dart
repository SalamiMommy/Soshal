// Import test for ffi wrapper: zap
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import zap compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
