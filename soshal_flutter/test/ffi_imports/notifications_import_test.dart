// Import test for ffi wrapper: notifications
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import notifications compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
