// Import test for ffi wrapper: marketplace
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import marketplace compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
