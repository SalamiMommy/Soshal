// Import test for ffi wrapper: moderation
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import moderation compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
