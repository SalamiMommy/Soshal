// Import test for ffi wrapper: streaming
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import streaming compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
