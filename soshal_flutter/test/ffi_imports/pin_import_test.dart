// Import test for ffi wrapper: pin
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import pin compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
