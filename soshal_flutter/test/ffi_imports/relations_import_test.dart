// Import test for ffi wrapper: relations
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import relations compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
