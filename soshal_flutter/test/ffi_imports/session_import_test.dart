// Import test for ffi wrapper: session
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import session compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
