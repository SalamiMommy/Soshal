// Import test for ffi wrapper: auth
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import auth compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
