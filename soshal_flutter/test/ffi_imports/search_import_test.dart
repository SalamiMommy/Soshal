// Import test for ffi wrapper: search
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import search compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
