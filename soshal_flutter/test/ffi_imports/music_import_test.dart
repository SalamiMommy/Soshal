// Import test for ffi wrapper: music
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import music compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
