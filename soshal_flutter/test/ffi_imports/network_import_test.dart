// Import test for ffi wrapper: network
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('import network compiles', () {
    TestWidgetsFlutterBinding.ensureInitialized();
    expect(true, isTrue);
  });
}
